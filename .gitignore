# Rust build artifacts
/target/

# Generated training data + intermediate model checkpoints (regenerable via
# fpc-train). Only champion.bin is tracked — it's include_bytes!'d into fpc-wasm.
/data/*.bin
!/data/champion.bin

# Inter-agent comms scratch
/comms/

# macOS
.DS_Store

# bench compiled artifacts
bench/bench_rust
bench/bench_go
bench/.venv
bench/bench_c
bench/bench_goengine
bench/bench_engine_c
